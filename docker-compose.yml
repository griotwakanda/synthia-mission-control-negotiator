version: '3.9'
services:
  mission-control:
    image: node:22-alpine
    working_dir: /app
    command: sh -c "npm install && npm run dev"
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - SIMULATION_MODE=true
    volumes:
      - ./:/app
